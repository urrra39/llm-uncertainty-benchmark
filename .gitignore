__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Intermediate run artifacts: large, and regenerable by `make all`.
data/cache/
data/raw/
data/artifacts/
data/*.parquet
data/pilot/

# results.json and figures/*.png are deliberately NOT ignored. They are the
# output of the project, they are what the README quotes, and a reader who
# clones this repo should see the numbers without owning a CPU-hour. The pilot's
# results file is ignored, because it is scaffolding rather than a result.
results_pilot.json
figures/*.pdf

# HuggingFace model/dataset downloads
hf_home/
*.safetensors
*.bin

.DS_Store
*.log
